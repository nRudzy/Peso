{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - Peso{% endblock %}

{% block body %}
<div class="space-y-8">
    <!-- En-tête du dashboard -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <h1 class="text-3xl font-bold text-gray-900">Tableau de bord</h1>
        <p class="text-gray-600 mt-2">Bienvenue, {{ app.user.email }}</p>
    </div>

    <!-- Grille des composants -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <!-- Composant Vue WeightProgressChart -->
        <div id="weight-progress-chart">
            <weight-progress-chart :user-id="{{ app.user.id }}"></weight-progress-chart>
        </div>
        
        <!-- Composant Vue JournalEntryForm -->
        <div id="journal-entry-form">
            <journal-entry-form @entry-created="refreshChart"></journal-entry-form>
        </div>
    </div>
</div>
{% endblock %} 